/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['E:/Desktop/ace360/public/images/navbar'], // allow external logo
    },
  };
  
  module.exports = nextConfig;
  